# Go
/bin/
/dist/
/board-server
/board-client
*.test
coverage.out

# Node / frontend
node_modules/
web/node_modules/
.pnpm-debug.log
pnpm-debug.log

# Built frontend output is generated by `make build` / `pnpm --dir web build`.
# Keep the directory (with .gitkeep) so `//go:embed all:dist` always compiles.
web/dist/*
!web/dist/.gitkeep

# Secrets
.env
.env.*
!.env.example

# Local data / runtime
*.db
*.db-wal
*.db-shm
/tmp/
*.local
.DS_Store
__pycache__/
*.pyc
